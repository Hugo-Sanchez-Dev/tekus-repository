namespace Tekus.Providers.Application.Enum
{
    public enum ResponseCodeEnum
    {
        OK = 200,
        NO_CONTENT = 204,
        ERROR = 500,
        BAD_REQUEST = 400,
        UNAUTHORIZED = 401,
        NOT_FOUND = 404
    }
}